name: Test

on:
  pull_request:
    branches:
      - 'main'
      - '!renovate/**'
  push:
    branches:
      - 'main'
      - 'renovate/**'

jobs:
  test:
    uses: WillBooster/reusable-workflows/.github/workflows/test.yml@main
    with:
      non_self_hosted: true
